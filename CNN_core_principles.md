# CNN(Convolutional Neural Network)의 핵심 원리

## 1. CNN이란?

CNN(합성곱 신경망)은 이미지와 같은 격자 형태의 데이터를 처리하는 데 특화된 딥러닝 모델입니다. 생물학적 시각 피질의 구조에서 영감을 받아 설계되었습니다.

## 2. 핵심 구성 요소

### 2.1 합성곱 층 (Convolutional Layer)
- **필터(Filter) 또는 커널(Kernel)**: 작은 크기의 가중치 행렬
- **동작 원리**: 입력 이미지를 필터가 슬라이딩하면서 합성곱 연산 수행
- **특징 추출**: 엣지, 텍스처, 패턴 등의 시각적 특징을 자동으로 학습
- **파라미터 공유**: 동일한 필터를 전체 이미지에 적용하여 파라미터 수를 대폭 감소
- **지역 연결성**: 각 뉴런이 입력의 일부 영역(receptive field)만 연결

```
입력 이미지 * 필터 = 특징 맵(Feature Map)
```

### 2.2 풀링 층 (Pooling Layer)
- **목적**: 특징 맵의 크기를 줄이고 계산량 감소
- **주요 방법**:
  - Max Pooling: 영역 내 최댓값 선택
  - Average Pooling: 영역 내 평균값 계산
- **효과**:
  - 불변성 제공 (translation invariance)
  - 과적합 방지
  - 연산량 감소

### 2.3 활성화 함수 (Activation Function)
- **ReLU(Rectified Linear Unit)**: 가장 많이 사용
  - `f(x) = max(0, x)`
  - 빠른 학습 속도
  - Gradient vanishing 문제 완화
- **기타**: Leaky ReLU, ELU, Swish 등

### 2.4 완전 연결 층 (Fully Connected Layer)
- CNN의 마지막 부분에 위치
- 추출된 특징들을 조합하여 최종 분류 수행
- 일반 신경망과 동일한 구조

## 3. CNN의 핵심 원리

### 3.1 지역적 수용 영역 (Local Receptive Field)
각 뉴런은 전체 입력이 아닌 일부 영역만 바라봅니다. 이는 이미지의 지역적 특징(엣지, 코너 등)을 효과적으로 감지합니다.

### 3.2 가중치 공유 (Weight Sharing)
동일한 필터를 이미지 전체에 적용함으로써:
- 파라미터 수를 대폭 감소
- 위치 불변성(translation invariance) 확보
- 같은 특징이 이미지 어디에 있든 감지 가능

### 3.3 계층적 특징 학습 (Hierarchical Feature Learning)
- **초기 층**: 낮은 수준의 특징 (엣지, 색상, 텍스처)
- **중간 층**: 중간 수준의 특징 (형태, 부분 패턴)
- **깊은 층**: 높은 수준의 특징 (객체 전체, 의미론적 특징)

### 3.4 공간적 계층 구조 (Spatial Hierarchy)
여러 합성곱 층과 풀링 층을 거치면서:
- 공간 해상도는 감소
- 특징 맵의 채널(depth)은 증가
- 더 넓은 수용 영역을 확보

## 4. CNN의 학습 과정

### 4.1 순전파 (Forward Propagation)
1. 입력 이미지가 합성곱 층을 통과하며 특징 추출
2. 활성화 함수 적용
3. 풀링으로 크기 축소
4. 이 과정을 여러 번 반복
5. 완전 연결 층에서 최종 출력 생성

### 4.2 역전파 (Backpropagation)
1. 손실 함수 계산 (예: Cross-Entropy)
2. 기울기 계산
3. 가중치 업데이트 (SGD, Adam 등의 옵티마이저 사용)
4. 필터의 가중치가 최적의 특징을 추출하도록 학습

## 5. CNN의 주요 장점

### 5.1 파라미터 효율성
- 가중치 공유로 파라미터 수 대폭 감소
- 완전 연결 신경망 대비 훨씬 적은 파라미터로 더 좋은 성능

### 5.2 위치 불변성 (Translation Invariance)
- 객체가 이미지의 어느 위치에 있든 인식 가능
- 풀링 층이 이를 더욱 강화

### 5.3 계층적 표현 학습
- 자동으로 특징을 학습 (Feature Engineering 불필요)
- 단순한 특징에서 복잡한 특징으로 점진적 학습

### 5.4 확장성
- 더 깊은 네트워크 구성 가능
- 다양한 크기의 입력 처리 가능

## 6. 대표적인 CNN 아키텍처

### 6.1 역사적 발전
- **LeNet-5 (1998)**: 최초의 성공적인 CNN, 손글씨 인식
- **AlexNet (2012)**: ImageNet에서 큰 성공, 딥러닝 붐 촉발
- **VGGNet (2014)**: 작은 3x3 필터를 깊게 쌓는 구조
- **GoogLeNet (2014)**: Inception 모듈 도입
- **ResNet (2015)**: Skip connection으로 매우 깊은 네트워크 가능
- **MobileNet (2017)**: 경량화된 모바일 특화 구조
- **EfficientNet (2019)**: 깊이, 너비, 해상도를 균형있게 조정

### 6.2 최신 트렌드
- **Vision Transformer (ViT)**: Attention 메커니즘 활용
- **ConvNeXt**: 현대화된 CNN 구조

## 7. CNN의 응용 분야

### 7.1 컴퓨터 비전
- 이미지 분류 (Image Classification)
- 객체 탐지 (Object Detection)
- 시맨틱 세그멘테이션 (Semantic Segmentation)
- 얼굴 인식 (Face Recognition)

### 7.2 의료 영상
- X-ray, CT, MRI 분석
- 질병 진단 및 예측

### 7.3 자율 주행
- 차선 감지
- 보행자 및 차량 탐지

### 7.4 기타
- 비디오 분석
- 자연어 처리 (1D CNN)
- 음성 인식

## 8. CNN의 수학적 표현

### 합성곱 연산
```
(I * K)(i, j) = ΣΣ I(i+m, j+n) × K(m, n)
```
- I: 입력 이미지
- K: 커널(필터)
- *: 합성곱 연산

### 출력 크기 계산
```
Output Size = (Input Size - Filter Size + 2×Padding) / Stride + 1
```

## 9. 실무에서의 고려사항

### 9.1 하이퍼파라미터
- 필터 크기: 3×3, 5×5, 7×7 등
- 필터 개수: 채널 수 결정
- 스트라이드(Stride): 필터 이동 간격
- 패딩(Padding): 경계 처리 방법

### 9.2 정규화 기법
- Batch Normalization: 학습 안정화
- Dropout: 과적합 방지
- Data Augmentation: 데이터 증강

### 9.3 최적화
- 학습률 스케줄링
- 가중치 초기화
- 옵티마이저 선택 (Adam, SGD 등)

## 10. 결론

CNN은 다음의 핵심 원리를 통해 이미지 처리에서 혁신을 가져왔습니다:

1. **지역적 연결성**: 이미지의 지역 패턴을 효과적으로 포착
2. **가중치 공유**: 파라미터 효율성과 위치 불변성 확보
3. **계층적 학습**: 단순한 특징에서 복잡한 특징으로 자동 학습
4. **풀링을 통한 추상화**: 공간적 크기 감소와 불변성 증가

이러한 원리들이 결합되어 CNN은 컴퓨터 비전의 핵심 기술이 되었으며, 다양한 실무 응용에서 뛰어난 성능을 보여주고 있습니다.
